/** GPU-safe cap — nearby troffer RectAreaLights only */
export const MAX_PANEL_LIGHTS = 20;
/** Panels stay lit within this radius even off-screen — avoids motion-sensor feel */
export const LIGHT_KEEP_RADIUS = 42;
