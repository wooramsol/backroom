/** GPU-safe cap — pooled troffer pairs (down + plenum up) */
export const MAX_PANEL_LIGHTS = 24;
/** Nearby panels stay lit even when behind the camera (stops rotate-in-place flicker) */
export const LIGHT_KEEP_RADIUS = 20;
