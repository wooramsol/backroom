/** GPU-safe cap — pooled RectAreaLights (each one is expensive) */
export const MAX_PANEL_LIGHTS = 40;
/** Only assign lights to panels within this radius of the player */
export const LIGHT_POOL_RADIUS = 20;
